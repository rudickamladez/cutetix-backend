name: Docker

on:
  push:
    branches: ["*"]
  pull_request:
    types: [opened, reopened, synchronize]
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set safe TAG from branch
        id: prep
        run: echo "TAG=$(echo '${{ github.ref_name }}' | sed 's/[^A-Za-z0-9]/-/g')" >> $GITHUB_OUTPUT

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ghcr.io/${{ github.repository }}:${{ steps.prep.outputs.TAG }}
          # volitelné cache:
          cache-from: type=gha
          cache-to: type=gha,mode=max
#  deploy-production:
#    runs-on: ubuntu-latest
#    needs: build-and-publish
#    if: github.ref == 'refs/heads/master'
#    environment: "production"
#    steps:
#      - name: Execute deployment on SSH
#        uses: appleboy/ssh-action@v0.1.10
#        with:
#          host: ${{ secrets.SSH_HOST }}
#          username: ${{ secrets.SSH_USERNAME }}
#          key: ${{ secrets.SSH_KEY }}
#          port: ${{ secrets.SSH_PORT }}
#          script: ${{ secrets.SCRIPT }}
#  deploy-dev:
#    runs-on: ubuntu-latest
#    if: github.ref != 'refs/heads/master'
#    environment: "dev"
#    steps:
#      - name: Execute deployment on SSH
#        uses: appleboy/ssh-action@v0.1.10
#        with:
#          host: ${{ secrets.SSH_HOST }}
#          username: ${{ secrets.SSH_USERNAME }}
#          key: ${{ secrets.SSH_KEY }}
#          port: ${{ secrets.SSH_PORT }}
#          script: ${{ secrets.SCRIPT }} ${{ github.head_ref || github.ref_name }}

